the file launch_global.desktop goes into ~/.config/autostart (this launches the launch_global.sh)
the file launch_global.sh goes into /SherpaHighLevel/scripts (this launches all nodes)
launchWasp_10x are for windows GS computer to launch all scripts and debug from the GS